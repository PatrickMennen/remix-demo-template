import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1474A4',
    },
    secondary: {
      main: '#189383',
    },
    error: {
      main: '#FA5A4B',
    },
  },
});

export default theme;
